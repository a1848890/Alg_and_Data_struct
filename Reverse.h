#pragma once
#include<string>

using std::string;

class Reverse{
    private:

    public:
        Reverse();
        int reverseDigit(int digit_to_reverse);
        string reverseString(string string_to_reverse);
};

